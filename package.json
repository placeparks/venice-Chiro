{
  "name": "spineai",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --sourcemap --format=iife --target=es2018 --define:process.env.NODE_ENV=\\\"production\\\" --outfile=dist/app.js",
    "dev": "esbuild src/app.jsx --bundle --sourcemap --format=iife --target=es2018 --define:process.env.NODE_ENV=\\\"development\\\" --outfile=dist/app.js --watch"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}

